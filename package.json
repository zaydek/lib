{
	"name": "@zaydek/lib",
	"version": "0.4.0",
	"license": "MIT",
	"scripts": {
		"build": "rm -rf dist && yarn tsc && cp package.json README.md dist",
		"link-dist": "yarn build && cd dist && yarn link",
		"unlink-dist": "yarn unlink",
		"publish-dist-dry-run": "yarn build && cp package.json README.md dist && cd dist && npm publish --dry-run && cd .."
	},
	"peerDependencies": {
		"react": "^16.8.6",
		"react-dom": "^16.8.6"
	},
	"devDependencies": {
		"@types/react": "^16.9.55",
		"prettier": "^2.1.2",
		"typescript": "^4.0.5"
	}
}
