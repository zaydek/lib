{
	"name": "@zaydek/lib",
	"version": "0.5.0",
	"license": "MIT",
	"files": [
		"dist/"
	],
	"scripts": {
		"test": "jest",
		"build": "yarn test && rm -rf dist && yarn tsc",
		"publish-dry-run": "yarn build && npm publish --dry-run"
	},
	"peerDependencies": {
		"react": "^16.8.6",
		"react-dom": "^16.8.6"
	},
	"devDependencies": {
		"@babel/preset-env": "^7.12.1",
		"@babel/preset-typescript": "^7.12.1",
		"@types/jest": "^26.0.15",
		"@types/react": "^16.9.55",
		"jest": "^26.6.3",
		"prettier": "^2.1.2",
		"typescript": "^4.0.5"
	}
}
