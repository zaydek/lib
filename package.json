{
	"name": "@zaydek/lib",
	"version": "0.1.0",
	"license": "MIT",
	"scripts": {
		"build": "rm -rf dist && yarn tsc",
		"link-dist": "yarn build && cp package.json README.md dist && cd dist && npm link",
		"unlink-dist": "npm unlink",
		"publish-dist-dry-run": "yarn build && cp package.json README.md dist && cd dist && npm publish --dry-run",
		"publish-dist": "yarn build && cp package.json README.md dist && cd dist && npm publish --access public"
	},
	"peerDependencies": {
		"react": "^16.8.6",
		"react-dom": "^16.8.6"
	},
	"devDependencies": {
		"@types/react": "^16.9.55",
		"prettier": "^2.1.2",
		"typescript": "^4.0.5"
	}
}
